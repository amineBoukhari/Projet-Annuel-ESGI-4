{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "docker:dev": "docker build -f Dockerfile.dev -t frontend . && docker run -it -p 5173:5173 -v $(pwd):/app -v /app/node_modules frontend",
    "docker:prod": "docker build -f Dockerfile.prod -t frontend . && docker run -p 5173:5173 frontend"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4"
  }
}
